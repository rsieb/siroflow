<%= notice %>
    <% @assignments.each do |assignment| %>
<%= assignment.context[0..9] %>/<%= assignment.status %>: <%= assignment.name %> <%= assignment.url %> <%= assignment.planned_start.strftime("%m-%d") unless assignment.planned_start.nil? %>
    <% end %>

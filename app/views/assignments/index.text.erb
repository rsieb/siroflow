<%= notice %>
    <% @assignments.each do |assignment| %>
<%= assignment.context %>/<%= assignment.status %>: <%= assignment.name %> <%= assignment.url %> <%= assignment.planned_start.strftime("%m-%d") unless assignment.planned_start.nil? %>
    <% end %>
